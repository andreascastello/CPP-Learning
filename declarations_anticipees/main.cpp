int main(int argc, char const *argv[])
{
    /*
        On utilise les includes dans les .cpp alors que dans les .h, on utilise une declaration anticipée
        afin d'éviter la dépendances bidirectionnelles dans les .h
    */
    return 0;
}
